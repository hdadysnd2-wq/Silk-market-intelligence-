requests>=2.31
pandas>=2.0
# --- اختياري · OPTIONAL (core imports & runs without these) ---
# واجهة REST عبر FastAPI (api.py):  uvicorn api:app
# fastapi>=0.110
# uvicorn>=0.29
# واجهة Streamlit (for app.py):  streamlit run app.py
# streamlit>=1.30
# وكيل Google Trends (silk_trends_agent):
# pytrends>=4.9
